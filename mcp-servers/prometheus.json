{
    "mcpServers": {
        "prometheus": {
            "description": "Query Prometheus metrics and time-series data using PromQL. Retrieve metric values, run range queries, and inspect monitoring targets and alert rules.",
            "command": "uv",
            "args": [
                "--directory",
                "/home/theo/projects/giantswarm-helpers/prometheus-mcp-server",
                "run",
                "src/prometheus_mcp_server/main.py"
            ],
            "env": {
                "PROMETHEUS_URL": "http://localhost:8080/prometheus",
                "ORG_ID": "giantswarm"
            }
        }
    }
}
